/*
 * 文字列のコピー
 * @param  hl コピー先
 * @param  de コピー元
 */
function strcpy(hl,de) {
    using(hl,de) {
        while() {
            // 文字列終了
            if(*hl == '\0') return;
            a = *de;
            *hl = a;
            hl++;
            de++;
        }
    }
}
